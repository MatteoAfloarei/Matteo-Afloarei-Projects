import random
import socket
charachters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*()-+=_{}[]|:;\"'<>,.?/"
input("Do you want to crack a password? (Y/N): ")
def generate_password(length):
    password = ""
    for i in range(length):
        password += random.choice(charachters)
    return password
def check_password(password):
    # This is a placeholder function. In a real implementation, you would check the password against a database or use an API.
    return password == "password123"
password_length = int(input("Enter the length of the password to crack: "))
while True:
    password = generate_password(password_length)
    print(f"Trying password: {password}")
    if check_password(password):
        print(f"Password found: {password}")
        break

# Input the base and height of a triangle
base = float(input("Enter the first lenght of the base of the triangle "))
height = float(input("Enter the height of the triangle: "))
#Calculate the area 
area = 0.5 * base * height

print(f"The area of the triangle is: {area}") 